export const getCod = (state) => state.weatherData.cod;
export const getMessage = (state): string => state.weatherData.message;
export const getCityName = (state): string => state.weatherData.cityName;
export const getTemperatureUnits = (state): string => state.weatherData.temperatureUnits;
